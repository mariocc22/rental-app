// validates if user is signed in
function isUserSignedIn() {

    return true
}

// validate if user can 
function canUserPost() {
    // validate if user phone number is available
    return true
}

function canUserBook() {
    // validate if user phone number is available
    return true
}

function isPlaceValid(placeId) {
    // validate place id in database -> check if place id exists

    return true
}


export { isUserSignedIn, canUserBook, canUserPost, isPlaceValid }